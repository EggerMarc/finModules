#include "header.h"

int main() {
  // linspace(0, 10, 1);
  //  printv(linspace(0, 0.1, 10));
  // std::vector<std::vector<int>> vec = {{1, 2, 3}, {1, 2, 3}};
  std::vector<int> vec = {1, 2, 3, 4};
  printv(vec);
  std::cout << std::endl;
  return 0;
}
